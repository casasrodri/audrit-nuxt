export default defineNuxtRouteMiddleware((to, from) => {
    // const jwt = useCookie('jwt', {
    //     httpOnly: true,
    //     secure: true,
    // })

    // console.log('jwt', jwt)

    // if (!jwt.value && to.path !== '/login') {
    //     return navigateTo('/login')
    // }
})
